<h1>Edit the <%= @tweet.content %> Tweet</h1>

<%= form_for @tweet do |f| %>
  <div>
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>
  
  <%= f.submit "Update Tweet!" %>
<% end %>

<%= link_to "Back to the list!", tweet_path %>